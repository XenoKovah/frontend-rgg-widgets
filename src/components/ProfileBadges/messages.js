import { defineMessages } from '@edx/frontend-platform/i18n';

const messages = defineMessages({
  'rgg.profile.badges.heading': {
    id: 'rgg.profile.badges.heading',
    defaultMessage: 'Earned Badges',
    description: 'Heading for the section on the user profile page that lists the badges the user has earned.',
  },
});

export default messages;
